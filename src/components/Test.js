import React from 'react'

const Test = () => {
  const name = 'Lex'
  const obj = {
    one: 'one',
    two: 'two',
  }

  console.log('Hello')

  return (
    <div>
      name:
      <h1>{name}</h1>
    </div>
  )
}

export default Test
